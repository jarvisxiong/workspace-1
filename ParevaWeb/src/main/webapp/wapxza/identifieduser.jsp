<%
// String domainName = System.getProperty(domain + "_url");
//      user = umeuserdao.authenticateUser(msisdn, "", 3, dmn);  
//      if(user!=null && club!=null && mobileclubdao.isActive(user, club)){
//      uid=user.getUnique().trim();
//             
//          response.sendRedirect("http://" + domainName + "/" + "?id=" + user.getWapId());
//          return;
//      }
//        String zane="";
//      if(msisdn!=null && msisdn.trim().length()>0)
//                   {
//          //Kohera koheranetwork = new Kohera();
//	//long koherabeforeauthenticate =  System.currentTimeMillis();
//       //	String token = kohera.authenticate();
//       //zane=kohera.getNetworkName(token, msisdn);
//      }
//	//campaigndao.log("global_wap_header", "xhtml", uid, msisdn, null, domain, campaignId, club.getUnique(),"IDENTIFIED", 0, request, response,zane);
//        ses.setAttribute("sdc_msisdn_param", msisdn);
        
 %>

